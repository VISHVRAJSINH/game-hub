import { Input } from "@chakra-ui/react";

const SearchInput = () => {
  return (
    <Input
      size="md"
      borderRadius={6}
      placeholder="Search games"
      variant="filled"
    />
  );
};

export default SearchInput;
